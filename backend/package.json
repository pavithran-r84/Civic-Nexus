{
  "name": "civic-nexus-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for Civic Nexus with CORS support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server db.json --port 3001 --cors"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
